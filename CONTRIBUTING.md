# 贡献指南

最有价值的贡献不是加资产，而是**加判断**。一份写清了适用条件、代价和反模式的选型矩阵，比十个技能条目有用。

## 三种贡献

| 类型 | 放哪 | 什么时候 |
|---|---|---|
| 新增决策入口 | `stages/<阶段>/DECIDE.md` | 有一类反复出现的选型拉扯还没被覆盖 |
| 新增登记卡 | `registry/<name>.md` | 某个现成资产值得被决策矩阵指向 |
| 新增自有资产 | `catalog/` | 现成的都不够用，必须自己造 |

优先级从上到下。**先问"现成的够不够用"，够用就写登记卡,不要重复造。**

## 新增一个决策入口

在 `stages/<阶段>/` 下建 `DECIDE.md`，七节结构缺一不可：

1. **要回答的问题** —— 用问句列出这个时刻的实际拉扯
2. **判断依据** —— 决定答案的几个轴，按权重排序，说明哪个权重最高
3. **选型矩阵** —— 表格：方案 / 适用条件 / 代价 / **反模式**
4. **推荐路径** —— 缺乏更多信息时的默认，以及改变默认的信号
5. **交给 agent 执行时的差异** —— 与人类独自操作时结论不同的地方
6. **常见误判** —— 具体的错误做法及其后果
7. **落地资产** —— 指向 `registry/` 或 `catalog/` 的具体条目

**反模式那一列是必填的。** 只说"什么时候用"而不说"什么时候别用"的矩阵，读者无法排除选项，等于没给判断。

写完在 `catalog/skills/sdlc/<名>/` 建配套路由技能，正文保持 20 行以内 —— 它只负责指路，判断在 DECIDE.md 里。

## 新增一个登记卡

```markdown
---
name: <唯一标识>
origin: marketplace | external | local
provider: <插件 id 或来源>
asset: <资产名>
updated_at: "YYYY-MM-DD"
---

## 何时用
（从哪个 DECIDE.md 的哪个判定结果过来，以及什么时候**不该**用它）

## 安装
（各 agent 下的安装方式；不适用的写"尚未验证"，别编）

## 版本
（由谁管理、去哪查）
```

两条硬规则：

**一、登记卡绝不放进 `catalog/skills/`。** `install.sh` 会把那里的目录挂载成真技能，登记卡的 description 会直接和上游原技能抢触发词，越帮越忙。

**二、登记卡不复制上游版本号。** Claude Code plugin 由 `installed_plugins.json` 管，gstack 有自己的 update-check，本库由 git 管。抄一份版本号进来，它当天就开始漂移成假数据 —— 只记"版本由谁管、去哪查"。

## description 怎么写

它同时驱动自动触发和检索，**按搜索查询来写，不要写成标题**。

塞进用户真实会说的话。不要写"设计系统选型指南"，要写"用哪个设计系统、要不要用 shadcn、UI 怎么做才不像模板"。

新增前先看一眼已有技能的 description，**不要和它们抢同一批触发短语** —— 这是技能库最常见的退化方式。

## 提交前

```bash
./install.sh          # 重新挂载，确认没有报错
./bin/check-update.sh # 确认脚本仍可运行
```

M2 之后这里会有 `sdlc check` 做 schema 与触发冲突校验。目前靠人工检查。

## 版权

- 外部文章原文不要提交，`library/inbox/` 已在 `.gitignore` 中。可以提交的是你自己写的摘要卡片（`library/notes/`），必须附来源链接与日期。
- 不要整段搬运他人内容进 `catalog/`。要么写登记卡指过去，要么用自己的话重写并注明出处。
- 提交即表示同意你的贡献以 MIT 发布。

## 语言

- 代码标识符、代码注释、CLI 输出用英文
- `.md` 文档用中文
