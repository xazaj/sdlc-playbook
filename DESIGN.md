# DESIGN.md

本文件约束 `site/` 的全部界面。生成或修改任何界面前先读它，并用一句话复述方向。

## 方向

一份证据先行的报告：全宽的十二栏版面，单色为主，靠排版、对齐与密度变化分组，不靠卡片与色块。

## 参照对象

Vercel 公开的品牌规范 <https://vercel.com/design.md> 及其 Geist 语义体系。本站采用它的**规则与数值**，不引用它的品牌样式表，不使用它的字标与三角标——那些是 Vercel 的品牌资产，第三方站点使用即构成冒用。token 名与实现均为本库自有。

判断某个改动是否成立，问一句：它读起来像一份可审计的报告，还是像一个产品落地页。后者即走偏。

## 禁止

- 卡片、嵌套面板、投影、假纵深。分组只用间距、对齐与规则线。
- 徽章、胶囊、圆角标签承载普通元数据。
- 全大写的眉标与装饰性小标签，装饰性章节编号，破折号作分隔。
- 渐变、光晕、纹理、玻璃拟态、彩色侧边条。
- 居中英雄区后接三栏卡片网格；重复的指标方块。
- 仅因为数值有利就把它变绿。颜色只在承载状态或数据含义时出现，且必须同时有非颜色线索。
- 看起来可点、实际无效的控件。
- 自述性段落。不解释站点自己怎么用。

## 上限

**字体三家族。** `Geist` 承担正文、标题与控件，`Geist Mono` 承担版本号、日期、路径与 prompt，`Noto Sans SC` 承担中文。均带系统回退。

**色彩单色为主。** 灰阶取 Geist 语义值，另有三个语义色，仅在承载含义时使用：

| 记号 | 值 | 用途 |
|---|---|---|
| `--bg` | `oklch(1 0 0)` | 页面底 |
| `--bg-2` | `oklch(0.984 0 0)` | 次级面（代码块、表头） |
| `--text` | `oklch(0.205 0 0)` | 正文 |
| `--text-2` | `oklch(0.42 0 0)` | 次级正文 |
| `--text-3` | `oklch(0.59 0 0)` | 元信息 |
| `--border` | `oklch(0 0 0 / 0.08)` | 常规线 |
| `--border-strong` | `oklch(0 0 0 / 0.44)` | 强调线 |
| `--focus` | `oklch(57.61% 0.2508 258.23)` | 焦点环 |
| `--warn` | `oklch(52.79% 0.1496 54.65)` | 评估待复核 |
| `--err` | `oklch(54.99% 0.232 25.29)` | 评估需复核 |

`--warn` 与 `--err` 只用于新鲜度，且始终与文字同时出现，不单独承载信息。

## 尺度

间距取 4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 / 64，不出现刻度外的数值。圆角只有 6px 与 8px 两档。

字号九档，各有其位：display 48、页标题 40、标题 32、章节 24、小节 20、导语 18、正文 16、紧凑 14、元信息 13。行高分别为 56 / 48 / 40 / 32 / 26 / 28 / 24 / 20 / 18。字重只用 400、450、500、600 四档。不创造第十档字号或第五档字重。

版面十二栏，平板六栏，窄屏四栏。内容区最宽 1440px，左右留白 40px，页眉页脚与规则线通栏。正文占六到七栏，宽度不超过 68 个字符；表格、对照与判断矩阵可占满十二栏。

节奏：组内 8 到 16，组间 24 到 32，章节转折 48 到 64。每个间距只有一个所有者，不靠一次性 margin 修补。

线只有两种粗细，各自承担一件事：分节线 `oklch(0 0 0 / 0.18)`，行分隔线 `oklch(0 0 0 / 0.08)`。不要用同一根线同时表达两种边界。

密度是分组手段，不是排版余量。论述区用正文节奏（16 / 24，段间 16），证据区收紧（14 / 20，行间 12），两者的密度差本身就是分组信号。同类可比数据全站一套排法：标签在左、数值在右、等宽数字，表格中的数字列右对齐且表头同向。

## 交互

默认静止。动效只用于解释状态变化或确认操作。

键盘可达是硬要求：`⌘K` 或 `/` 唤起搜索，`↑` `↓` 移动，`⏎` 打开，`Esc` 关闭；首屏提供跳转到主内容的链接；所有可交互元素有可见的 `:focus-visible` 状态。筛选就地生效，安装 prompt 就地复制。

数字一律使用等宽数字（`tabular-nums`），同一列的版本号与日期必须对齐。
