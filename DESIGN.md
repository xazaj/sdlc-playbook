# DESIGN.md

本文件约束 `site/` 的全部界面。生成或修改任何界面前先读它，并用一句话复述方向。

## 方向

Vercel 亮色产品界面：白底、Geist 灰阶、1px `#eaeaea` 边线的卡片与胶囊、黑色主按钮、`#0070f3` 链接色——读起来像 Vercel dashboard，不像审计报告，也绝不像营销落地页。

## 参照对象

Vercel 公开的品牌规范 <https://vercel.com/design.md>、Geist 语义体系与 Web Interface Guidelines <https://vercel.com/design/guidelines>。本站采用它的**组件词汇与数值**（灰阶、边线、圆角、按钮层级、表格卡片化），不引用它的品牌样式表，不使用它的字标与三角标——那些是 Vercel 的品牌资产，第三方站点使用即构成冒用。token 名与实现均为本库自有。

判断某个改动是否成立，问一句：它在 Vercel 的 dashboard 里出现过吗。没有对应物的东西（渐变横幅、光晕、玻璃拟态）不进来。

## 组件词汇

| 场景 | 形态 |
|---|---|
| 数据表格（清单） | 1px 边线卡片，表头 `#fafafa` 灰底，行分隔线 `#eaeaea`，行 hover 灰底 |
| 安装 prompt | 边线卡片 + 头部栏（`#fafafa`、左侧语言标签、右侧复制按钮），正文等宽字体灰底 |
| 元数据（版本、日期、形态、许可） | 胶囊徽章：24px 高、999px 圆角、1px 边线、12px 字 |
| 主操作（复制安装 prompt） | 黑底白字按钮，600px 高度 36px，hover `#333` |
| 次操作（卡内复制） | 白底 1px 边线按钮，微投影，hover 边线加深 |
| 筛选 | 下划线 tab：选中文字黑色 + 2px `#0070f3` 下划线，计数等宽字体 |
| 搜索 | 页头胶囊触发器（含 ⌘K kbd）+ 圆角 12 遮罩对话框 |
| 链接 | 正文与导航链接 `#0070f3`，hover `#3291ff`；表格条目名保持黑色 hover 才变色 |

## 禁止

- 渐变、光晕、纹理、玻璃拟态、彩色侧边条、投影造纵深（按钮与对话框的微投影除外，那两层是 Geist 自带的）。
- 居中英雄区后接三栏卡片网格；重复的指标方块。
- emoji 当图标；自造图标。
- Inter 或 Roboto 当唯一字体。拉丁文与数字用 `Geist`，等宽用 `Geist Mono`，中文交给平台原生 UI 字（见上限）。
- 蓝色只用于链接、选中态与焦点环。`--warn` 与 `--err` 只用于评估新鲜度，且始终与文字同现，不单独承载信息。
- 看起来可点、实际无效的控件。
- 自述性段落。不解释站点自己怎么用。

## 上限

**字体两家族加一层中文回退。** `Geist` 承担拉丁文、数字与控件，`Geist Mono` 承担版本号、日期、路径与 prompt。中文不下载网络字体，交给平台原生 UI 字：苹果用 `PingFang SC`，Windows 用 `Microsoft YaHei`，都没有时才落到 `Noto Sans SC` 网络字体兜底——平台自带时它不会触发下载。

理由是平台原生中文字为各自的屏幕渲染调过，比统一下载一套更好看，且中文网络字体即便按 unicode-range 分片，在中文为主的页面上仍会造成可见的字体切换。

**色彩。** 灰阶取 Geist 语义值（`#111/#444/#666` 文字层级，`#eaeaea/#cbcbcb` 边线两级，`#fafafa` 次级面），另有 `--link #0070f3`、`--focus #0070f3` 与新鲜度用的 `--warn #92400e`、`--err #b91c1c`。文字不使用 `#888`：它在白底上只有 3.54:1，不过正文对比度线；状态色是深琥珀与深红，白底 4.5:1 以上。颜色只在承载含义时出现。

## 尺度

间距取 4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 / 64，不出现刻度外的数值。圆角三档：6px（按钮、输入、kbd）、8px（卡片）、12px（遮罩对话框）；胶囊 999px。

字号九档，各有其位：display 48、页标题 40、标题 32、章节 24、小节 20、导语 18、正文 16、紧凑 14、元信息 13。行高分别为 56 / 48 / 40 / 32 / 26 / 28 / 24 / 20 / 18。字重只用 400、500、600 三档。不创造第十档字号或第四档字重。

版面十二栏，平板六栏，窄屏四栏。内容区最宽 1200px（发布值），左右留白 32px，页眉与页脚的线通栏。正文占六到七栏，宽度不超过 68 个字符；表格、对照与判断矩阵可占满十二栏，安装 prompt 卡可用满本栏。

节奏：组内 8 到 16，组间 24 到 32，章节转折 48 到 64。每个间距只有一个所有者，不靠一次性 margin 修补。

页面是一整块连续画布，卡片只给「自身成组」的东西：清单表格与安装 prompt。论述与判断矩阵仍是排版与规则线，不套卡——全页皆卡等于没有卡。真正的章节转折才画一条 `#cbcbcb` 的线，且它是一个元素而不是 section 的 border——border 会跟着版心的内边距一起延伸，与表格的线对不齐。

密度是分组手段，不是排版余量。论述区用正文节奏（16 / 24，段间 16），证据区收紧（14 / 20，行间 12），两者的密度差本身就是分组信号。同类可比数据全站一套排法：标签在左、数值在右、等宽数字，表格中的数字列右对齐且表头同向。

## 中文适配

发布值的行高与字距是为拉丁文调的，直接套用在中文上不成立。以下三处是记录在案的偏离，不是随手改：

- **正文行距抬一档。** 界面与表格等密集处保持 16 / 24，正文段落用 16 / 28。中文没有小写字母的升降部，1.5 倍行距在长段落上偏挤。
- **标题负字距收到几乎不可见。** 拉丁文标题惯用的 −0.02 至 −0.03em 会挤压汉字的字面框，本站最大只用 −0.012em，20px 及以下一律 `normal`。
- **开启中日韩排版特性。** `text-spacing-trim: normal` 收掉全角标点在行首行尾与相邻处的空档，`text-autospace: normal` 在中文与拉丁文、数字之间插入四分之一空。本站正文大量夹杂 `agent`、`DESIGN.md`、`prompt`，这一条的收益最直接。

## 秩序

用户来这里是为了拿到资产。条目表格与可复制的 prompt 是视觉中心且必须靠上；判断依据、口径说明、分类导航都是补充，一律排在其后或缩到元信息档。开篇不超过一句标题加一句导语，不得占据一屏。

页面有三个模板：列表、明细与文章。列表只有首页一个，用 tab 切换时刻；明细实例化为条目页；文章实例化为文章页与归档页，见下节。新增列表页之前先问它除了换一组筛选条件之外还提供什么，只是筛过的表格不构成一个页面。

列表保留选择所需的最少列：条目（名称与一句话）、时刻、形态、评估版本、评估日期。来源不进列表，它对选择无贡献。

时刻进列表，因为筛选器与列回答的不是同一个问题：筛选器是控件，说的是「只看这一批」；列是数据，说的是「这一条属于哪里」。在「全部」视图下后者没有别的出口。评估版本与日期各占一列而不是堆成一格，是为了让它们各自成一条右对齐的竖线——一千余像素宽的表格靠竖线扫读，不靠横线。

状态不出字。「现行」是绝大多数行的状态，逐行重复等于零信息，反而稀释了 † 与 ‡。列表里状态只保留字形，落在日期右侧一个 1ch 的定宽槽里，需要复核的行因此在右边缘自己跳出来；完整的词留给条目页与屏幕阅读器（条目页里它在胶囊徽章内，颜色之外有字重线索）。


## 文章

第三类资产是给人读的，不进 agent 上下文，也不参与触发。

**归档页（/articles/）。** 按定稿日倒序，一条一行：标题、一句 summary、mono 日期。它对「新列表页提供什么」的回答是时间轴与阅读入口，因此不用表格、不加筛选器。导航的「文章」一项随首批文章出现。

**文章页（/articles/<name>/）。** 阅读优先，语汇取自当下的长文排版：

- 不设边注栏。备注稀疏时它读成正文与导航之间的割裂带（初版试过即撤）。出处进头部元数据行，成段出处进「来源与边界」，工具与佐证链接就地内联。右栏沿用条目页的位置与跨度。
- 头部一行 Geist Mono 元数据：日期 · 阅读时长（按每分钟 400 字估）· 来源域名（链到原文）· 作者。
- 页头下缘一条 2px 阅读进度线，颜色 `--link`，随滚动增长；无过渡，滚动本身就是状态。
- 小节标题不编号；引文与代码块沿用条目页样式。结尾不写来源节：页面在正文后渲染来源卡，1px 边线卡片一行——作者、原文链接，译文加一枚「中文全译」胶囊标记，仅此而已。related 的登记卡以胶囊徽章列在右栏。

## 交互

默认静止。动效只用于解释状态变化或确认操作；过渡只许 `color`、`background-color`、`border-color` 三属性，且 `prefers-reduced-motion` 下全关。不得使用 `transition: all`。

键盘可达是硬要求：`⌘K` 或 `/` 唤起搜索，`↑` `↓` 移动，`⏎` 打开，`Esc` 关闭；首屏提供跳转到主内容的链接；所有可交互元素有可见的 `:focus-visible` 状态（`#0070f3` 焦点环）。筛选就地生效并写进 URL hash，安装 prompt 就地复制。

数字一律使用等宽数字（`tabular-nums`），版本号与日期各自在本列右对齐，表头同向。
