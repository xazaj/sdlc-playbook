# M0 格式验证日志

记录 `stages/30-coding/DECIDE.md` 与路由技能 `sdlc-coding-process` 在真实使用中的表现。M2 编写工具时，只解决此处记录在案的痛点。

## 记录方法

每次实际用到（或应触发而未触发）时补一条：

```
### YYYY-MM-DD 场景一句话
- 触发：是 / 否（应触发而未触发也要记录）
- 建议是否有用：有用 / 没用 / 部分
- 摩擦点：
```

## 疑点处理状态

| # | 疑点 | 处理 |
|---|---|---|
| 1 | 路径硬编码，换机器即失效 | **已解决。** 约定固定路径 `~/.sdlc`，所有引用统一使用该路径；`install.sh` 负责挂载。仓库可以物理上位于任何位置，用软链指向 `~/.sdlc` 即可 |
| 2 | DECIDE.md 篇幅（71 / 85 行） | **暂不处理。** 合计约 2000 token，agent 全文读取尚可接受。拆分到 references 要等到确有痛点，否则只是凭空增加一层间接 |
| 3 | 第七节"落地资产"内容为空 | **保留。** 它目前起待办清单的作用，指明下一批应建立的条目。有资产后自然填满 |
| 4 | 单个路由技能测不出触发区分度 | **已解决。** 增加 `20-design` 决策入口与 `sdlc-design` 技能，两个技能的 description 覆盖不同触发词，可以测区分度 |
| 5 | MANIFEST.yaml 未创建 | **暂不创建。** 目前没有任何东西消费它，写了只是负担。M2 有校验器时再加，届时为纯增量 |

## 新引入的待验证问题

- **两个路由技能是否会互相争夺触发。** 例如"这个界面怎么做"既可能是问设计方案，也可能是问开发流程。
- **软链方式在其他 agent 上是否可用。** 目前只挂载了 Claude Code。pi 有已知的软链重复注册问题（issue #3405），M3 需改用复制。

## 记录

### 2026-09-03 挂载完成
- 触发：未验证。技能列表在会话启动时加载，本次会话挂载的技能要到下次新会话才生效。
- 建议是否有用：未验证。
- 摩擦点：无。挂载本身只用了一条 `ln -sfn`。

### 2026-09-03 跨机器改造
- 触发：仍未验证，原因同上。
- 摩擦点：`install.sh` 需要从 SKILL.md 中用 sed 提取 name 字段来决定软链名，原因是技能目录名（`coding-process`）与技能名（`sdlc-coding-process`）不一致。要么接受这一步 sed，要么让目录名与技能名一致。后者更简单，但目录会失去 `sdlc/` 这层分组。暂时接受 sed。

### 2026-09-03 实测两项未知
- **`~` 路径可直接读取。** 用 Read 工具读取 `~/.sdlc/stages/20-design/DECIDE.md` 成功返回内容。路径约定成立，不需要在安装时展开为绝对路径。疑点关闭。
- **技能挂载后当前会话即可见。** `install.sh` 执行完毕，本会话立刻收到两条技能通报，不需要重开会话。原先 `install.sh` 与 README 中"需开新会话"的提示有误，已修正。
- **仍未验证：自动触发。** 技能被"发现"与被"自动激活"是两回事。需要在真实提问中确认 description 是否足够精准，以及两个技能是否会互相争夺。

### 2026-09-03 确立编排层定位
调研本机现有资产后的三个发现，直接改变了实现：

1. **Claude Code 已有完整的资产登记机制。** `known_marketplaces.json` 记录来源仓库，`installed_plugins.json` 记录 version、gitCommitSha、lastUpdated。gstack 也自带 `last-update-check` 与升级技能。在本库复制一份版本号必然漂移为错误数据，因此登记卡只记录版本由谁管理、到哪里查询。
2. **登记卡不能放入 `catalog/skills/`。** `install.sh` 会把该目录软链为技能，登记卡的 description 会与上游技能争夺触发词。已单独建立 `registry/`，纯文档，不挂载。
3. **vendored 暂不实现。** 目前两个外部资产（superpowers、gstack）都有自己的分发与更新机制，没有落地到本库的理由。等遇到散装资产（GitHub 上单个 SKILL.md、没有版本机制）再做，届时约定放在 `vendor/<name>/`。

版本检查使用 `bin/check-update.sh`，由路由技能在用法第 0 步调用：只在真正用到本库时检查，不安装全局 hook，对其他项目无侵入。节流 4 小时。

- 摩擦点：当时没有 git remote，`check-update.sh` 走的是"无远端"分支。真正的检查逻辑（fetch 与 rev-list）尚未执行过，推送远程仓库后需复验。**已于同日复验，见下条。**

### 2026-09-03 开源发布与更新检查复验
仓库已公开：https://github.com/xazaj/sdlc-playbook （MIT）。

`bin/check-update.sh` 三条路径全部实测通过：

| 场景 | 预期 | 实测 |
|---|---|---|
| 已是最新 | 静默，写入时间戳 | 通过。无输出，stamp 已写入 |
| 节流窗口内重复调用 | 直接返回，不发网络请求 | 通过。无输出 |
| 落后一个 commit | 提示落后数与 pull 命令 | 通过。输出 `sdlc: knowledge base is 1 commit(s) behind` |

发布前审计：无本机绝对路径，无邮箱泄露。`~/.gstack/last-update-check` 是 gstack 的通用约定路径，非本机特定，保留。

- 仍未验证：**自动触发。** 两个路由技能能被"发现"已确认，但 description 是否精准到足以自动激活、两者是否会互相争夺，仍需在真实使用中观察。这是 M1 的主要输入。

### 2026-09-03 v2 方案评审
v2 提出改为人翻阅的资产目录，配安装 prompt、新鲜度脚本与 GitHub Pages 站点。评审结论：保留 v1 骨架，吸收两个机制，其余暂缓。

- 采纳：登记卡增加 `evaluated_version`、`evaluated_at` 与"安装 prompt"节。两张现有登记卡已改写，模板同步进 CONTRIBUTING.md 与 AGENTS.md。
- 暂缓：删除路由层、每周 freshness 检查、站点。重启条件写在 v2 文档文首。
- 摩擦点：安装 prompt 中的 AGENTS.md 片段是英文，登记卡正文是中文。这是按"写进目标项目的内容用英文"的语言约定处理的，读者第一次看到时可能困惑，已在 CONTRIBUTING 的 prompt 规则中补充说明。

### 2026-09-03 新增需求与规格时刻（15-define）
用户问「增加一个需求分类，盘点编写/评审/加细节的资产」。两轮分析后定为方案 B：统一「动手之前把要做的事写清楚」这一段，六个子活动（摄入澄清、产品级、变更级、加细节、评审、逆向），标题「需求与规格」。

- **资产按源头热度分诊，广受欢迎的先发卡。** 三个上游经 GitHub API 核实：obra/superpowers 281k★、anthropics/skills 173k★（官方）、garrytan/gstack 131k★。首批登记五张卡：ears-syntax（doc）、superpowers-brainstorming、superpowers-writing-plans、anthropic-doc-coauthoring、gstack-plan-review（四件套+autoplan 合一卡）。PM-Make、make-PRD、interaction-prd、demo-to-product-model 搜索零足迹，印证上一轮「建仓当天、1-2★」的实测，标观望不发卡；本机自建资产（11 个需求类 agent、hlb-prd 两个 skill）按约定待登记，先收敛重复再发卡。
- **与 30-coding 划界。** 「要不要写规格」的判定留在 build（其判断轴是错误暴露周期）；define 只管写到多细、按什么句式、谁来审（判断轴是涉众数量×需求确定性×执行方式）。30-coding 的 SDD 行已从「待登记」改为指向 define 的两张 superpowers 卡。
- **验证：** `cd site && npm run build` 通过（12 页，含五张新卡与 define 分类页）；dist 首页图例渲染为「五个时刻」，确认动态化生效；`./install.sh` 挂载 sdlc-requirements 成功，本机共 3 个 sdlc 技能。
- 摩擦点：新增一个时刻要动六处写死「四个」的地方（CATEGORY_IDS、三份分类文件的 order、index.astro 图例标题、CONTRIBUTING、categories/_TEMPLATE、site/README）。图例标题已改为按 categories.length 生成，CONTRIBUTING 写明新增步骤。此痛点记为 M2 校验器候选（order 连续性与枚举-文件一致性可机检）。
- 仍未验证：sdlc-requirements 的自动触发，以及它与 sdlc-coding-process 在「要不要先写规格」一词上的争夺。description 已刻意分工（前者管形态句式评审，后者管流程），待真实使用观察。
