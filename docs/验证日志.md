# M0 格式验证日志

记录 `stages/30-coding/DECIDE.md` 与路由技能 `sdlc-coding-process` 在真实使用中的表现。
M2 写工具时，只解决这里记录在案的痛点。

## 怎么记

每次实际用到（或该用却没被触发）时补一条：

```
### YYYY-MM-DD 场景一句话
- 触发：是 / 否（该触发却没触发也要记）
- 建议是否有用：有用 / 没用 / 部分
- 摩擦点：
```

## 疑点处理状态

| # | 疑点 | 处理 |
|---|---|---|
| 1 | 路径硬编码，换机器就废 | **已解决**。约定固定路径 `~/.sdlc`，所有引用统一走它；`install.sh` 负责挂载。仓库可以物理上在任何位置，用软链指到 `~/.sdlc` 即可 |
| 2 | DECIDE.md 篇幅（71 / 85 行） | **暂不处理**。合计约 2000 token，agent 全文读取尚可接受。拆到 references 要等真的感到痛，否则是凭空多一层间接 |
| 3 | 第四节"落地资产"是空的 | **保留**。它现在起待办清单的作用，指明下一批该建什么条目。有资产后自然填满 |
| 4 | 单个路由技能测不出触发区分度 | **已解决**。加了 `20-design` 决策入口与 `sdlc-design` 技能，两个技能的 description 覆盖不同触发词，现在可以测区分度了 |
| 5 | MANIFEST.yaml 未创建 | **暂不创建**。目前没有任何东西消费它，写了纯属负担。M2 有校验器时再加，届时是纯增量 |

## 新引入的待验证问题

- **两个路由技能会不会互相抢触发** —— 例如"这个界面怎么做"既可能想问设计方案，也可能想问开发流程。
- **软链方式在其他 agent 上是否可用** —— 目前只挂了 Claude Code。pi 有已知的软链重复注册问题（issue #3405），到 M3 要改用复制。

## 记录

### 2026-09-03 挂载完成
- 触发：未验证。技能列表在会话启动时加载，本次会话挂载的技能要下次开新会话才生效。
- 建议是否有用：未验证。
- 摩擦点：无（挂载本身只用了一条 `ln -sfn`）。

### 2026-09-03 跨机器改造
- 触发：仍未验证（本次会话挂载的技能要下次开新会话才生效）。
- 摩擦点：`install.sh` 需要从 SKILL.md 里 sed 出 name 字段来决定软链名。这是因为技能目录名（`coding-process`）和技能名（`sdlc-coding-process`）不一致。要么接受这点 sed，要么让目录名与技能名一致 —— 后者更简单，但目录会失去 `sdlc/` 这层分组。暂时接受 sed。

### 2026-09-03 实测两项未知
- **`~` 路径可直接读取** —— 用 Read 工具读 `~/.sdlc/stages/20-design/DECIDE.md` 成功返回内容。路径约定成立，不需要在安装时展开成绝对路径。疑点关闭。
- **技能挂载后当前会话即可见** —— `install.sh` 跑完，本会话立刻收到两条技能通报，不需要重开会话。原先 install.sh 和 README 里"需开新会话"的提示是错的，已改。
- **仍未验证：自动触发。** 技能被"发现"和被"自动激活"是两回事。要在真实提问中确认 description 是否足够精准，以及两个技能会不会互相抢。
